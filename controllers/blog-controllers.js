// Prob don't need

const express = require("express");

const router = express.Router();

// render views based on the routes hit



router.get("/", function (req, res) {
    // set the data you want to display into a data const and pass it along with the page name in res.render
    res.render("home",)
})



// /login

// /signup

// /logout

// /dashboard

